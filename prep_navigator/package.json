{
  "name": "prep-navigator-console",
  "version": "1.1.0",
  "description": "Continuity — a PrEP navigator console for monitoring patient lifecycle and preventing gaps in care. Storage-agnostic: SQLite locally, Firestore on Google Cloud Run, with live cross-client sync.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node --experimental-sqlite server.mjs",
    "dev": "node --watch --experimental-sqlite server.mjs",
    "start:firestore": "STORE=firestore node --experimental-sqlite server.mjs"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.11.0"
  }
}
